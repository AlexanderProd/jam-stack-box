version: "3"

services:
  server:
    build:
      context: .
      dockerfile: ./server/Dockerfile
    container_name: jamstackbox_server
    ports:
      - 4757:3000
    volumes:
      - ~/.jamstackbox/sites-public:/sites-public/
      - ~/.jamstackbox/db:/db/
